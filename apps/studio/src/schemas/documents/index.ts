import entities from './entities';
import globalComponents from './global_components';
import global_settings from './global_settings';
import pages from './pages';

export default [...global_settings, ...entities, ...pages, ...globalComponents];
